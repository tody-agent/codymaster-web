# Brainstorm Output: OpenViking & v4.7.0 Documentation Upgrade
Generated by: cm-brainstorm-idea
Date: 2026-04-03

## Qualified Problem
**For:** Developers and PMs using CodyMaster
**Who:** Read the documentation at code.todyle.com/docs
**The:** Documentation is currently on v4.3.0
**That:** Needs to reflect the massive v4.7.0 upgrade (OpenViking, Context Backbone v5, Self-Healing Pipeline)
**Unlike:** The old v4.3.0 docs which only mention JSON storage and 35 skills.
**Our approach:** Conduct a deep restructure of `index.md`, `architecture.md`, and `brain.md` to elevate OpenViking and the semantic backbone to their rightful central place.

### Root Cause(s):
1. **Technical root cause**: The system evolved drastically in the last week (v4.3.0 to v4.7.0), swapping JSON storage for SQLite/FTS5 and OpenViking.
2. **Product root cause**: The landing pages and diagrams do not mention the new pipeline (68+ skills vs 35, auto-installation via pip3, Context Bus).

### Impact if NOT addressed:
- **User impact**: Users will not know about OpenViking, the new `cm://` URI scheme, token budget enforcement, or the huge expansion from 35 to 68 skills.

## 9 Windows Analysis

| | PAST | PRESENT | FUTURE |
|---|---|---|---|
| **SUPER-SYSTEM** | Agent frameworks with goldfish memory. | Agentic workflows with integrated Vector DBs and unified context. | Fully autonomous multi-agent swarms with shared semantic graphs. |
| **SYSTEM** | CodyMaster v4.3.0. JSON-based memory, 35 skills, discrete execution. | CodyMaster v4.7.0. OpenViking native, SQLite+FTS5, Context Backbone v5, Self-Healing Pipeline, 68+ skills. | CodyMaster as an OS-level background semantic layer for codebases. |
| **SUB-SYSTEM** | `index.md`, `brain.md`, `architecture.md` hardcoded to v4.3.0 paradigms. | Docs are increasingly disjointed from the reality of the codebase. | Docs are dynamically updated and accurately reflect system architecture. |

## Options Evaluated

### Option A: Incremental Patch
**Approach:** Quick find-and-replace of "35 skills" to "68+ skills" and adding a paragraph about OpenViking in the architecture doc.
**Effort:** S (~15 min)
**Pros:** Fast, minimal disruption.
**Cons:** Leaves outdated architectural diagrams and concepts (like JSON files TTL) intact, confusing users.

### Option B: Deep Restructure & OpenViking Elevation (Recommended)
**Approach:** Rewrite the Core Concepts in `architecture.md` and `brain.md` to explain the Context Backbone v5 (SQLite/Viking `StorageBackend`), `cm://` URIs, and the Self-Healing Pipeline. Update `index.md` Mermaid diagrams and skill counts.
**Effort:** M (~45 min)
**Pros:** Accurately explains the new Hub-and-Spoke and 5-tier memory with the correct tech stack. Clarifies the SQLite vs Viking backend swap.
**Cons:** Takes more effort to ensure accuracy across markdown files.

### Option C: Complete Redesign
**Approach:** Split `architecture.md` and `brain.md` into micro-articles. Add new dedicated pages for "OpenViking Backend Setup" and "Self-Healing AI".
**Effort:** L (~2 hours)
**Pros:** Extremely detailed.
**Cons:** Might overcomplicate the standard documentation flow. Needs Vitepress sidebar reconfiguration.

## Recommendation

**Choose Option B: Deep Restructure & OpenViking Elevation**

### Why Option B:
1. It targets the exact problem (outdated system components) without rebuilding the VitePress site structure.
2. It correctly elevates OpenViking from a hidden backend to the highlighted 'Core Feature' it is in v4.7.0.
3. It updates the Mermaid diagrams to reflect the real architecture (Self-Healing components).

## Next Steps for cm-planning:
1. Update `docs/index.md` (Version, skill count 68+, Mermaid graph to include Self-Healing and Context Backbone).
2. Update `docs/architecture.md` (Explain `StorageBackend` abstraction, SQLite `context.db` vs `VikingBackend`, CM:// URIs, Context Bus).
3. Update `docs/brain.md` (Explain semantic vector search powered by OpenViking vs local SQLite FTS5, Token Budget enforcement).
4. Update `docs/deployment.md` (Explain Zero-Touch CLI installation and `pip3 install openviking`).
